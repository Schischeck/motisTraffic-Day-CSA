#include "gtest/gtest.h"

namespace motis {
namespace connectionchecker {

TEST(connectionchecker, finds_annotated_connections) {
  ASSERT_TRUE(true);
}

} // namespace connectionchecker
} // namespace motis