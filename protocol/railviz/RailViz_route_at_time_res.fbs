include "railviz/RailVizTrain.fbs";

namespace motis.railviz;

table RailViz_route_entry {
  departure_id: uint;
  departure_station_name: string;
  train_departure : RailVizTrain;
}

table RailViz_route_at_time_res {
  train_num : string;
  train_identifier : string;
  classz: int = -1;
  route : [RailViz_route_entry];
}
