namespace motis.intermodal;

table Station {
  name: string;
  eva: string;
  lat: double;
  lng: double;
}

table StationList {
  stations: [Station];
}

table IntermodalGeoIndexResponse {
  coordinates: [StationList];
}
