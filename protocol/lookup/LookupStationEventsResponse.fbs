include "lookup/LookupCommon.fbs";

namespace motis.lookup;

table StationEvent {
  id_event: IdEvent;

  type: EventType;
  train_nr: uint;
  line_id: string;

  time: ulong;
  schedule_time: ulong;
}

table LookupStationEventsResponse {
  events: [StationEvent];
}
