include "Service.fbs";

namespace motis.loader;

enum RuleType : byte {
  THROUGH, MERGE_SPLIT
}

table Rule {
  type: RuleType;
  service1: Service;
  service2: Service;
  eva_1: int;
  eva_2: int;
}

table RuleGraph {
  rules: [Rule];
}